{
  "name": "trifold",
  "private": true,
  "version": "0.1.0",
  "description": "Define an operation once, ship it as an HTTP API, a CLI, and an MCP server",
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dallascrilley/trifold.git"
  },
  "bugs": {
    "url": "https://github.com/dallascrilley/trifold/issues"
  },
  "homepage": "https://github.com/dallascrilley/trifold#readme",
  "license": "MIT",
  "author": "Dallas Crilley",
  "keywords": [
    "mcp",
    "cli",
    "openapi",
    "typescript",
    "hono",
    "zod",
    "boilerplate",
    "operation-registry"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "validate": "bash scripts/validate.sh",
    "scan:ai-slop": "bash scripts/scan-ai-slop.sh",
    "scan:ai-slop:all": "bash scripts/scan-ai-slop.sh --all",
    "dev:api": "pnpm --filter @trifold-app/api dev",
    "dev:cli": "pnpm --filter @trifold-app/cli start",
    "dev:mcp": "pnpm --filter @trifold-app/mcp start",
    "dev:mcp:http": "pnpm --filter @trifold-app/mcp start -- --http",
    "snapshot:update": "pnpm --filter @trifold/openapi snapshot && pnpm --filter @trifold/adapters-mcp snapshot",
    "scaffold": "tsx scripts/scaffold-product.ts",
    "openapi:import": "tsx scripts/openapi-import.ts"
  },
  "devDependencies": {
    "@trifold/openapi": "workspace:*",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
